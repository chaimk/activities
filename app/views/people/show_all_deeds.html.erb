<% provide(:title, @person.name) %>

<h1><%= @person.name %></h1>

<h2>Deed history:

<%= render partial:'people/person_deeds_list', object_to_check: @person_deeds %>

<br>
<div font-size=".8em">[<%= link_to "Show this week only", @person %>]</div>
</div>