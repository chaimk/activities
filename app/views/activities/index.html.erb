<% provide(:title, 'Show All Activites') %>
<h1>List of All Activities</h1>

<table border="1" cellpadding="5">
	<tr>
		<th>ID</th>
		<th>Name</th>
		<th>Category</th>
		<th>Points</th>
		<th># of Deeds</th>
		<th>Created</th>
		<th>Modified</th>
	</tr>

	<% @activities.each do |t| %>
	<tr>
		<td><%= t.id %></td>
		<td><%= link_to t.name, activity_path(t.id) %></td>
		<td><%= t.category %></td>
		<td><%= t.points %></td>
		<td><%= t.deeds.length %></td>
		<td><%= t.created_at %></td>
		<td><%= t.updated_at %></td>
		<% if signed_in_as_admin? %>
			<td><%= link_to "Edit", edit_activity_path(t.id) %></td>
			<td><%= link_to "Delete", t, method: :delete, data: { confirm: "Are you sure?" } %></td>
		<% end %>
	</tr>
	<% end %>

</table>

<br><a href="/people">Show all people</a>

<% if signed_in_as_admin? %>
	&nbsp; | &nbsp; 
	<a href="/activities/new">Create new activity</a>
<% end %>